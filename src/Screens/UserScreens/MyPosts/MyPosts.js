import React from 'react';
import {View, Text} from 'react-native';

const MyPosts = props => {
    return(
        <View style={{flext:1, justifyContent:'center', alignItems:'center'}}>
            <Text style={{color:'#000'}}>My Posts will come here</Text>
        </View>
    );
};

export default MyPosts;