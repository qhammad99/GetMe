import React from 'react';
import {View, Text} from 'react-native';

const Settings = props => {
    return(
        <View style={{flext:1, justifyContent:'center', alignItems:'center'}}>
            <Text style={{color:'#000'}}>Setting screen</Text>
        </View>
    );
};

export default Settings;