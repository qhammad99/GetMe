import React from 'react';
import {View, Text} from 'react-native';

const MyPages = props => {
    return(
        <View style={{flext:1, justifyContent:'center', alignItems:'center'}}>
            <Text style={{color:'#000'}}>My pages</Text>
        </View>
    );
};

export default MyPages;